(ns mcp-tasks.cli.commands
  "Command implementations for the CLI.
  
  Thin wrappers around mcp-tasks.tools functions.")

;; TODO: Implement command functions (list, show, add, complete, update, delete)
