(ns mcp-tasks.cli.format-test
  "Tests for CLI output formatting."
  (:require
    [clojure.test :refer [deftest testing is]]
    [mcp-tasks.cli.format :as sut]))

;; TODO: Implement tests for format rendering
