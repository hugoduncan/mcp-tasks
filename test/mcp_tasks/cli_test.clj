(ns mcp-tasks.cli-test
  "Tests for the CLI main entry point."
  (:require
    [clojure.test :refer [deftest testing is]]
    [mcp-tasks.cli :as sut]))

;; TODO: Implement tests for CLI command routing
